# prompt_builder.py

def build_module_prompt(tech_task, manifest, module, interfaces, all_modules_code):
    # Передаём список существующих классов, чтобы избегать дублирования
    existing_classes = []
    try:
        data = json.loads(interfaces) if isinstance(interfaces, str) else interfaces
        for cls in data.keys():
            existing_classes.append(cls)
    except Exception:
        pass

    return f"""
Ты — ИИ-агент, который пишет библиотечный модуль Python для многомодульного проекта.

Твоя задача — написать модуль `{module['name']}`.

ТРЕБОВАНИЯ:
1. Если в других модулях есть подходящие классы, ИСПОЛЬЗУЙ их (импортируй и вызывай).
2. НЕ дублируй функциональность, которая уже реализована.
3. Добавляй новые классы/методы только если они действительно нужны.
4. НЕ используй print() в модулях (кроме main).
5. Все функции должны возвращать значения (не выводить).
6. Модуль должен быть самодостаточным и корректно импортироваться.
7. Модуль содержащий main.py должен быть точкой входа и вызывать другие модули. не должно быть несколько модулей, содержащих main.py.

ДОСТУПНЫЕ ИНТЕРФЕЙСЫ (классы и методы других модулей):
{interfaces}

Список уже существующих классов (не создавай дубликаты):
{existing_classes}

Техническое задание проекта:
{tech_task}

Сгенерируй ТОЛЬКО код модуля `{module['name']}`. Не добавляй пояснений, комментарии минимальны.
"""